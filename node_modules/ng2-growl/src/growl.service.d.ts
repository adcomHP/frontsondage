import { EventEmitter } from '@angular/core';
export declare enum Types {
    'ERROR' = 0,
    'WARN' = 1,
    'INFO' = 2,
    'SUCCESS' = 3,
}
export interface GrowlItem {
    heading?: string;
    message?: string;
    type: Types;
    id: number;
}
export interface Message {
    heading?: string;
    message?: string;
}
export declare class GrowlService {
    constructor();
    private timeout;
    OnAddMessage: EventEmitter<any>;
    OnRemoveMessage: EventEmitter<any>;
    static _createMessage(msg: Message | string): Message;
    private addMessage(msg, type);
    private addMessages(msg, type);
    addError(msg: Message | Message[] | string | string[]): void;
    addWarn(msg: Message | Message[] | string | string[]): void;
    addInfo(msg: Message | Message[] | string | string[]): void;
    addSuccess(msg: Message | Message[] | string | string[]): void;
}
